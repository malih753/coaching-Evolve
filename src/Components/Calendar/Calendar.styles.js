import styled from "styled-components";
export const CalendarWrap =styled.div`
.calenderHolder{
    width: 100%;
}
h4{
    color: var(--blue);
    display: flex;
    align-items: center;
    gap: 20px;
}

`;